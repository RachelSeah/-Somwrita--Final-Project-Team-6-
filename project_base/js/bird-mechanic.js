// js/bird-mechanic.js
// ─────────────────────────────────────────────────────────────
// BIRD MECHANIC — Person D
// This is where the bird animation lives.
//
// ┌─────────────────────────────────────────────────────────┐
// │  HOW TO ADD THE BIRDS                                   │
// │                                                         │
// │  1. Create a <canvas> element over the scene            │
// │  2. Use ctx.beginPath() / ctx.fill() to draw birds      │
// │  3. Move them with requestAnimationFrame                 │
// │  4. Reset position when they fly off screen             │
// │                                                         │
// │  REFERENCE:                                             │
// │  https://www.youtube.com/watch?v=ttz05d8DSOs            │
// │  Original p5.js sketch by Patt Vira                     │
// └─────────────────────────────────────────────────────────┘
// ─────────────────────────────────────────────────────────────

import { state } from './state.js';

export function initBirds() {

  // ── ADD YOUR BIRD ANIMATION HERE ─────────────────────────
  // Steps:
  // 1. Get the scene element:
  //    const scene = document.getElementById('scene');
  //
  // 2. Create a canvas and add it on top:
  //    const canvas = document.createElement('canvas');
  //    canvas.width  = 1455;
  //    canvas.height = 1087;
  //    canvas.style.cssText = 'position:absolute;inset:0;width:100%;height:100%;pointer-events:none;z-index:500;';
  //    scene.appendChild(canvas);
  //
  // 3. Draw and animate your birds using canvas 2D API

}
